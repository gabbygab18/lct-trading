*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Client handoff files, not source
/*.zip
/*.pdf

# Raw khmtools feed; the seed CSV and product photos built from it are committed
# so a fresh deploy (e.g. Laravel Cloud staging) has a full catalog.
/scripts/data/raw

# Impeccable review captures
/.impeccable/review
/public/images/products/gallery
